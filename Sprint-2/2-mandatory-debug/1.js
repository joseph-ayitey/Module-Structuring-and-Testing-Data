// Predict and explain first...
//  =============> write your prediction here
//The console will log "The sum of 10 and 32 is undefined"

function sum(a, b) {
  return;
  a + b;
}

console.log(`The sum of 10 and 32 is ${sum(10, 32)}`)

// =============> write your explanation here
  // The `return` statement is followed by a semicolon on the next line,
// so JavaScript automatically inserts a semicolon after `return`.
// This means the function returns `undefined` and never executes `a + b`.

// Finally, correct the code to fix the problem
//  =============> write your new code here


