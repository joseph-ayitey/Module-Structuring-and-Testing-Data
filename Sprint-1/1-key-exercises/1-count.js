let count = 0;

count = count + 1;

// Line 1 is a variable declaration, creating the count variable with an initial value of 0
// Describe what line 3 is doing, in particular focus on what = is doing
// Line 3 reassigns the count variable by taking its current value (0) and adding 1 to it, resulting in a new value of 1, which is then assigned back to the count variable using the = operator.

console.log(count); // This will output 1 to the console.
